apiVersion: v1
kind: Secret
metadata:
  name: {{ include "mongo.fullname" . }}
type: Opaque
data:
  {{- with .Values.deployment.secret }}
  {{- range $key, $value := . }}
  {{ $key }}: {{ $value | b64enc }}
  {{- end}}
  {{- end  }}